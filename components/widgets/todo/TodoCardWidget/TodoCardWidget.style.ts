import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  todoCardView: {
    borderWidth: 1,
    borderColor: '#cccccc',
    borderRadius: 8,
    padding: 16,
    flexDirection: 'row',
    justifyContent: 'space-between'
  },
  todoCardActionView: {
    gap: 8
  },
});
